version: '3.8'

services:
  cinesense:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cinesense-app
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - HOST=0.0.0.0
      - DATA_DIR=data/processed
    restart: unless-stopped
